export interface SDKRepository
{
  /*
  sendNewPlainMessage(to_address: string, content: string, subject: string): Promise
  sendNewInputMessage(to_address: string, content: string, subject: string): Promise
  replyRepos(refMsg, content, repos): Promise
  replyTo(ref, content, account_address, subject): Promise
  sendNextSyncRepoMsg(msg, repos, content, email): Promise 
  replyFromReference(msg, content, email): Promise 
  sendLoginMsg(msg, state, content): Promise
  sendIssueMsg(msg, repos): Promise 
  replyMsgError(msg, content): Promise 
  replyMsgSuccess(msg, content): Promise 
  sendSyncRepoMsg(msg, repos): Promise
  sendFirtsSyncRepoMsg(email, repos): Promise
  sendAccountsIssueMsg(msg, accounts): Promise 
  sendWelcomeMsg(to_address, state): Promise 
  sendNewMsg(to_address): Promise 
  sendNewMsgRepo(to_address, content, subject): Promise
  sendSyncAccountMsg(msg, state): Promise
  */
}